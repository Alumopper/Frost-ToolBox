<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FrostLeaf_ToolBox.Pages.TagManagerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FrostLeaf_ToolBox.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Background>
    <AcrylicBrush TintColor="White" TintOpacity="0.0" FallbackColor="White"/>
    </Page.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock Text="标签管理" Margin="30,30,0,0" FontSize="32" TextTrimming="CharacterEllipsis"/>
            <TextBlock Text="读取数据包中使用过的所有标签，并整合到一个文件中便于识别和管理" Margin="30,30,0,0" TextTrimming="CharacterEllipsis"/>
            <TextBlock Text="选择或者拖动文件夹或文件到此处开始读取" Margin="30,10,0,0" TextTrimming="CharacterEllipsis"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Content="选择" Margin="30,10,10,0"/>
            <Button Content="读取" Margin="30,10,10,0"/>
        </StackPanel>
    </Grid>
</Page>
